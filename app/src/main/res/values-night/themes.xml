<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SearchingProducts" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Colores primarios -->
        <item name="colorPrimary">@color/yellow_ml</item>
        <item name="colorPrimaryDark">@color/yellow_ml_dark</item>
        <item name="colorAccent">@color/blue_ml</item>

        <!-- Colores de fondo -->
        <item name="android:windowBackground">@color/dark_background</item>
        <item name="backgroundColor">@color/dark_background</item>

        <!-- Colores de texto -->
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/gray_300</item>

        <!-- Colores de tarjetas -->
        <item name="cardBackgroundColor">@color/dark_surface</item>

    </style>
</resources>